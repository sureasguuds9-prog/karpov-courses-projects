# Данные проекта 3

В `data/raw` находятся шесть исходных файлов задания: три таблицы пользователей
и три таблицы транзакций для `control_1`, `control_2` и `test`.

Разделитель CSV — `;`. Ноутбук загружает файлы из этой папки через относительный
путь `../data/raw`.
